<img
  <%= raw render_type == 'inline' ? "class='externalfilterinline'" : "" %>
  src="<%= wiki_external_filter_path(:macro => macro,
                                     :name => name,
                                     :index => 0) %>"
  alt="<%= source %>">
